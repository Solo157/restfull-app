replicaCount: 1

service:
  port: 8090
  targetPort: 8090
  type: ClusterIP

image:
  notificationRepository: ghcr.io/solo157/restfull-notification
  tag: latest
  pullPolicy: Always

app:
  notificationContainerPort: 8090
  notificationHealthPath: /notification/health