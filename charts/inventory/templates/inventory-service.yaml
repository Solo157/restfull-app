apiVersion: v1
kind: Service
metadata:
  namespace: auth-kuber-service-space
  name: {{ include "inventory-service.name" . }}-service
spec:
  selector:
    app: {{ include "inventory-service.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
