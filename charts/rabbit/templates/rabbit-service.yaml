apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
  namespace: auth-kuber-service-space
spec:
  type: ClusterIP
  ports:
    - port: 5672
      targetPort: 5672
      name: amqp
    - port: 15672
      targetPort: 15672
      name: management
  selector:
    app: rabbitmq