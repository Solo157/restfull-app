#apiVersion: apps/v1
#kind: Deployment
#metadata:
#  name: rabbitmq
#spec:
#  replicas: 1
#  selector:
#    matchLabels:
#      app: rabbitmq
#  template:
#    metadata:
#      labels:
#        app: rabbitmq
#    spec:
#      containers:
#        - name: rabbitmq
#          image: rabbitmq:3-management   # включает панель управления
#          ports:
#            - containerPort: 5672      # AMQP порт
#            - containerPort: 15672     # панель управления
#          env:
#            - name: RABBITMQ_DEFAULT_USER
#              value: "guest"           # пользователь по умолчанию
#            - name: RABBITMQ_DEFAULT_PASS
#              value: "guest"           # пароль по умолчанию
#          volumeMounts:
#            - name: rabbitmq-data
#              mountPath: /var/lib/rabbitmq
#      volumes:
#        - name: rabbitmq-data
#          persistentVolumeClaim:
#            claimName: rabbitmq-pvc